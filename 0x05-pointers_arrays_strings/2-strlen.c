#include "main.h"

/**
  * _strlen - main entry point.
  * @s: prints the length of a string.
  * Return: 0 or 1 On Succes Always.
  */

int _strlen(char *s)
{
	s[1000];
	int l;
		for (l = 0; s[l] != '\0'; l++)
			return (l);
}
