#include "variadic_functions.h"
#include <stdio.h>
#include <stdlib.h>
#include <string.h>

/**
  * print_all - main function for prints.
  * @format: the function formatter.
  *
  * Return: Always (0) for succes.
  */

void print_all(const char * const format, ...)
{
	va_start ap;
	va_end(ap);
	return (0);
}
